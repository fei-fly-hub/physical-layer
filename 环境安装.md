### 🟢 项目一：`python_5gtoolbox` (5G 物理层算法验证)

这是最容易上手的，纯 Python 环境，无需编译 C++。

#### 1. 安装步骤
为了避免 GitHub 网络中断，我们使用 **Gitee 导入法**（最稳定）或 **加速镜像**。这里直接使用加速镜像克隆：

```bash
# 1. 进入工作目录
cd /mnt/workspace/5G

# 2. 使用加速镜像克隆 (如果失败，请手动在 Gitee 导入后 clone)
git clone https://gitclone.com/github.com/hahaliu2001/python_5gtoolbox.git
cd python_5gtoolbox

# 3. 安装系统级 Python 依赖 (DSW 通常已包含，但确保万无一失)
apt-get update && apt-get install -y python3-pip python3-venv

# 4. 创建并激活虚拟环境 (推荐，避免污染全局环境)
python3 -m venv venv
source venv/bin/activate

# 5. 升级 pip 并安装 poetry (该项目推荐的依赖管理工具)
pip install --upgrade pip
pip install poetry

# 6. 使用 poetry 安装项目依赖 (这可能需要几分钟，因为它要编译一些 numpy/scipy 相关的包)
poetry install
```

#### 2. 验证与使用
该项目包含了大量测试用例，直接运行一个 LDPC 或 Polar 编码的测试来验证：

```bash
# 确保你还在虚拟环境中 (source venv/bin/activate)
# 运行 PDSCH (下行共享信道) 的相关测试脚本
cd py5gphy/nr_pdsch
python3 nr_pdsch.py
```
*预期结果*：终端会打印出编码、调制、OFDM 生成的日志，最后显示 `Test Passed` 或类似的成功信息。你可以打开 `nr_pdsch.py`，阅读其中的注释，尝试修改 `mcs` (调制编码策略) 参数重新运行，观察输出变化。

---

### 🟡 项目二：`gr-ieee802-11` (WiFi SDR 物理层)

**⚠️ 强烈警告**：在 Linux 上从源码编译 GNU Radio 及其依赖（Boost, IT++, UHD 等）极其痛苦且容易失败。**在云端环境，使用 Docker 是唯一推荐的生产力方案。**

#### 1. 安装步骤 (使用 Docker)
```bash
# 1. 确保你的 DSW 环境安装了 Docker (通常阿里云 DSW 已预装)
docker --version

# 2. 拉取预编译好的 gr-ieee802-11 Docker 镜像 (由社区维护，包含所有依赖)
docker pull bastibl/gr-ieee802-11

# 3. 启动容器 (以交互模式运行，并将当前目录挂载到容器内，方便你修改代码)
# 注意：因为没有 GUI，我们不加 -e DISPLAY 等图形界面参数，仅使用命令行
docker run -it --name wifi_sdr -v /mnt/workspace/5G:/workspace bastibl/gr-ieee802-11 /bin/bash
```
*此时你的终端提示符会变成类似 `root@<container_id>:/#`，说明你已进入 Docker 容器。*

#### 2. 验证与使用 (无头模式)
在容器内，你可以运行 Python 脚本来生成或处理 WiFi 信号，而无需打开 GNU Radio Companion (GRC) 图形界面。

```bash
# 在 Docker 容器内执行：
cd /workspace

# 克隆示例代码 (如果之前没在宿主机克隆)
git clone https://gitclone.com/github.com/bastibl/gr-ieee802-11.git
cd gr-ieee802-11/apps

# 运行一个自带的 WiFi 物理层仿真 Python 脚本 (无头模式)
# 这个脚本会生成 WiFi 信号，通过虚拟信道，然后尝试解调，并输出误码率 (BER)
python3 wifi_loopback.py
```
*预期结果*：终端会打印出类似 `Received X packets, Y errors` 的日志。你可以打开 `wifi_loopback.py`，尝试修改其中的 `channel_model` (如增加多径延迟)，观察误码率的变化。

*(退出容器输入 `exit`，下次进入使用 `docker start -i wifi_sdr`)*

---

### 🔴 项目三：`srsRAN` (工业级 4G/5G 协议栈)

我们将安装 **srsRAN_4G** (原 srsLTE)，因为它对硬件要求最低，且完美支持 **ZMQ (ZeroMQ)**，允许你在**没有真实 SDR 硬件**的云端服务器上，通过内存虚拟射频进行端到端 (EPC + eNB + UE) 的联调。

#### 1. 安装系统依赖
```bash
# 1. 更新并安装编译 srsRAN 必需的底层库
apt-get update
apt-get install -y cmake make gcc g++ pkg-config libfftw3-dev libmbedtls-dev libsctp-dev \
                   libzmq3-dev libconfig++-dev libboost-system-dev libboost-program-options-dev \
                   libboost-test-dev libboost-thread-dev libboost-filesystem-dev libuhd-dev
```

#### 2. 编译与安装 srsRAN_4G
```bash
# 1. 回到工作目录
cd /mnt/workspace/5G

# 2. 克隆 srsRAN 4G 仓库 (使用加速镜像)
git clone https://gitclone.com/github.com/srsran/srsRAN_4G.git
cd srsRAN_4G

# 3. 创建 build 目录并进入
mkdir build && cd build

# 4. 配置 CMake (关键：必须开启 ZEROMQ 和 禁用某些不需要的硬件驱动以加快编译)
cmake ../ -DENABLE_ZEROMQ=ON -DENABLE_SRSUE=ON -DENABLE_SRSENB=ON -DENABLE_SRS_EPC=ON

# 5. 编译 (使用多核加速，例如 -j4 表示用 4 个核心，根据你的 DSW 配置调整)
make -j4

# 6. 安装到系统
make install

# 7. 更新动态链接库缓存
ldconfig
```

#### 3. 验证与使用 (ZMQ 虚拟射频端到端测试)
srsRAN 提供了开箱即用的 ZMQ 测试脚本，模拟完整的 4G 网络。

```bash
# 1. 进入 srsRAN 的测试目录
cd /mnt/workspace/5G/srsRAN_4G/build

# 2. 运行官方提供的 ZMQ 端到端测试脚本
# 这个脚本会自动在后台启动 EPC(核心网)、eNB(基站)、UE(终端)，并通过 ZMQ 连接它们
# 最后会运行 iperf3 测试吞吐量
./srstest_zmq
```
*预期结果*：
1. 你会看到 `srsepc`, `srsenb`, `srsue` 依次启动的日志。
2. `srsue` 会显示 `RRC Connection Setup Complete`（表示终端成功接入基站）。
3. 最后会运行 `iperf3`，输出类似 `15.2 Mbits/sec` 的虚拟网络吞吐量测试结果。
4. 测试结束后，脚本会自动清理进程。

---
